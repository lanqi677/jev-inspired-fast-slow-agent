# 这个仓库里的源码是普通 JavaScript / Python / HTML，没有构建步骤。
# 统一 LF，避免 Windows 上 checkout 时把 CRLF 混进 .mjs / .py / .md。
* text=auto eol=lf

# 二进制
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.zip binary
*.woff binary
*.woff2 binary
