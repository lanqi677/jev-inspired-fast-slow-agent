# 补充：把"推理模型规划 + 快模型执行"跑通后，实测出的 8 个缺陷

> 承接 `browser-computer-use方案.md`。这一轮按你的三条意见改造了架构并**实际跑通**：
> 你提的 #1（候选收缩交给推理模型）和 #3（置信度只用于触发接管）都验证了，但**都和我原来的实现方式不一样**。
> 代码：`browser_jev/plan.mjs`（两阶段 agent）、`app2.html`（134 元素的噪声页）、`perceive.js`（加了 ctx / zone）。

---

## 0. 最终结果

| 项 | 值 |
|---|---|
| 页面元素 | **134 个**交互元素（25 行表格 × 3 按钮 + 导航/工具栏/筛选/分页） |
| 候选收缩 | **134 → 20（14.9%）**，由推理模型给出 |
| System 2（v4-pro 规划） | **1 次**，0.30 s，**$0.000855** |
| System 1（flash 执行） | **8 步**，1.05 s，**$0.002564** |
| **合计** | **1.35 s，$0.003419 / 任务** |
| 反思次数 | **0** |
| 最终状态 | **已提交 ✅** |

---

## 1. 你的三条意见，验证结果

### #1「候选收缩交给推理模型」——对，但**不能只给关键词，必须给排序**

**第一版实现失败了**，而且失败得很隐蔽：

```
keepIds  = J27…J134   （包含所有表格按钮）
keepLabels = 编辑 / 保存 / 提交 / …
▶ 候选收缩: 134 → 20  (14.9%)
```

数字看着很漂亮，但执行端紧接着做了 `filter(keep).slice(0, 20)` —— **按 DOM 顺序截断**，
前面的导航/工具栏/筛选栏正好占满 20 个名额，**所有「编辑」按钮被切没了**。

> **教训：收缩不是"按条件过滤后截断"，而是"按重要性排序后取前 N"。**
> 推理模型输出的 `keepIds` 必须是**有序的**，且排序在前的**保证进得来**。

### #2「工单→表单映射」——推理模型做得**完全正确**

System 2 的第一次输出：

```json
"fieldMap": {
  "标题":   "登录页报错",
  "经办人": "李娜",
  "优先级": "高",
  "状态":   "处理中",
  "描述":   "用户反馈登录接口在 token 过期后未刷新。复现: 打开登录页 → 等待 30 分钟 → 点击提交。控制台报 Uncaught TypeError…"
}
```

对照三层映射：

| 层 | 本案实例 | 结果 |
|---|---|---|
| T1 同名 | `title` → 「标题」 | ✅ |
| T2 异名 | `assignee` → 「经办人」 | ✅ |
| **T2 异值** | `priority: "P1"` → `"高"` | ✅ **正确转换** |
| T3 需要理解 | `description` → 「备注」 | ⚠️ 整段塞进去了，**没做摘要** |

**结论：T3 这类映射交给推理模型是对的**，但要在 prompt 里明确要求"提炼成短文本"，否则它会原文照搬（本例 88 个字符塞进备注框）。

### #3「置信度只用于触发接管」——**完全同意，而且我原来的阈值设计是错的**

实测到的置信度分布：

| 情境 | conf | 选择对吗 | 该不该拦 |
|---|---|---|---|
| 目标 #1042 在页面上**根本不存在** | **0.05** | 不知道 | ✅ **该拦** |
| 25 个一模一样的「编辑」，要挑对的 | 0.50–0.55 | **对** | ❌ **不该拦** |
| 面板已开，选经办人下拉框 | 0.55 / 0.35 | **对** | ❌ **不该拦** |
| 打开编辑面板 | 0.90–0.95 | 对 | 不该拦 |

我原来设的 `conf < 0.60 → 不执行`，**拦掉的正确决策比错误决策还多**。改成：

```js
if (conf < 0.15)  →  模型明确"不知道" → 升级给人
if (conf < 0.60)  →  照常执行，但执行后【强制比对界面是否变化】
else              →  执行
```

**真正的安全网不是模型的置信度，是代码侧的变化检测。** 因为它能区分两件置信度区分不了的事：
- 「点了但没达标」（界面变了，方向错）→ 可以换路径
- 「点了完全没反应」（界面没变）→ 必须换路径

> 一句话：**置信度是"分流校验强度"的信号，不是"允许/禁止执行"的开关。**

---

## 2. 跑通过程中实测出的 8 个缺陷

| # | 缺陷 | 症状 | 修法 |
|---|---|---|---|
| **F1** | 候选收缩按 DOM 顺序截断 | 收敛率 14.9% 很漂亮，但**要用的元素全被切掉** | 按推理模型的**排序**取，排序在前的一定进得来 |
| **F2** | 无"动作是否生效"检测 | 同一个「查询」按钮**连点 9 次** | **状态指纹 + 空操作检测**（比对 status/fields/alerts/dialogs/candidates） |
| **F3** | 无进展也烧完 10 步才反思 | 3 次反思 × 5575 token = **$0.0083 白花** | **连续 2 次空操作即提前反思** |
| **F4** | 反思时重发全量候选表 | 每次反思 5439–5834 input token | 反思改用**新鲜快照**；前缀缓存命中 5248（省了大半） |
| **F5** | 每步视图仍按 DOM 截断 | 面板打开后，**新出现的表单字段（DOM 靠后）被切掉** | **按优先级排序**：面板内 +100 / 表单控件 +50 / 命中计划关键词 +20 / 计划点名 +10 |
| **F6** | 目标不存在仍进入规划 | 目标 #1042 不在页面上，白烧 3 次反思 $0.007 | **确定性前置检查**（不花钱、最先跑）：目标 id 在不在候选的 `ctx` 里 |
| **F7** | 让快模型做"计划台账" | 反复重复计划的**第一步**（连点两次「编辑」） | **代码侧进度台账**（当前值 vs 目标值，逐项 ✅/⬜） |
| **F8** | 只"告诉"模型已完成，不够 | 代码说"经办人已完成"，模型照选不误 | **结构性移除**：已完成的字段、试过无效的元素，**直接从候选表拿掉**（和危险词拦截同一个原则） |

**F7/F8 是最重要的一条结构性教训**：

> **台账归代码，模型只回答"这件事由哪个元素完成"。**
> 凡是能用"当前值 vs 目标值"算出来的，就不该让模型记。

---

## 3. 另外两个真实新增的感知字段（真实页面必需）

| 字段 | 为什么必需 | 效果 |
|---|---|---|
| **`ctx`（所在行上下文）** | 25 行各有「编辑」按钮，没有行上下文，模型**不可能**知道哪个是 #1042 | 有了之后，第 1 步就以 **conf 0.90** 选对 `J116 行=#1042 登录页报错（批次 23）` |
| **`zone`（page / panel）** | 面板打开后，当前焦点是面板内元素，不是页面上那 100 个 | 每步候选排序的首要权重 |

实测里 `ctx` 决定性：**没有它，模型给 0.05（完全迷失）；有它，给 0.90。**

---

## 4. 修正后的成本结构

| 层 | 调用次数 | 花费 | 占比 | 可摊薄性 |
|---|---|---|---|---|
| System 2（v4-pro 规划） | 1 次/任务 | $0.000855 | **25%** | ✅ **同类型任务可缓存复用 → 归零** |
| System 1（flash 执行） | 8 步/任务 | $0.002564 | **75%** | 每步 $0.00032，随候选数线性涨 |
| **合计** | | **$0.003419** | | **≈ $3.4 / 1000 单** |

对比：
- 简单页面（11 候选、无规划层）：$0.00073/任务
- 真实噪声页面（134 元素、两阶段）：$0.00342/任务 → **4.7 倍**
- 其中**规划层只占 25%**，而且**同类型任务第二次跑就是 $0**

> 所以真正的成本杠杆是 **System 1 的步数 × 候选数**。
> 8 步里有 **2 步是浪费的**（重复点「提交」）；候选 20 个时单步 205 output token。
> **优化方向：减少无效步（F8 已改善）+ 把候选压到 ≤12。**

---

## 5. 修正后的设计规则（替换原方案 §5）

| # | 规则 | 依据 |
|---|---|---|
| R1 | 收缩 = **按重要性排序后取前 N**，绝不按 DOM 顺序截断 | F1 |
| R2 | **`ctx`（所在行）+ `zone`（面板/页面）是必需字段** | 实测 conf 0.05 → 0.90 |
| R3 | **每个动作后比对状态指纹**，界面没变 = 空操作 = 换路径 | F2 |
| R4 | **连续 2 次空操作立即反思**，不要烧完步数 | F3 |
| R5 | **规划前做确定性前置检查**（目标在不在页面上） | F6 |
| R6 | **进度台账由代码算**；模型只回答"哪个元素" | F7 |
| R7 | 已完成/已失效的选项**从候选表结构性移除**，不靠模型自觉 | F8 |
| R8 | 危险动作由**代码按标签拦截**（已有实证：模型会照指令去点「删除」） | 前一轮 |
| R9 | 置信度：`<0.15` 升级给人；`0.15–0.60` **执行+强制比对**；`>0.60` 直接执行 | F 全部 |
| R10 | 成功判定**由代码做**，模型的"完成=yes"只作参考 | 全部 |

---

## 6. 还没做的（优先级排序）

| 优先级 | 缺口 | 为什么重要 |
|---|---|---|
| 🔴 高 | **同一页面模板的计划缓存** | 规划层 25% 的成本可以归零；这是数据飞轮的起点 |
| 🔴 高 | **ECE / 风险-覆盖率曲线** | 现在阈值 0.15/0.60 仍是拍的。要测的是"低置信是否真的更容易错"，不是 ECE |
| 🟠 中 | **减少无效步**（8 步里 2 步浪费） | 直接的成本杠杆 |
| 🟠 中 | **备注摘要**（T3 映射要明确要求精炼） | 现在 88 字符原文进备注框 |
| 🟠 中 | **iframe / shadow DOM / 新标签页** | 真实页面必遇 |
| 🟡 低 | **错误恢复原语 D7** | 弹窗挡住、加载中、验证码 |
| 🟡 低 | **append-only 审计** | 每步快照/prompt/响应/动作 |

---

## 7. 一句话

> **跑通之后最重要的发现是：我一直在让快模型做它不该做的事。**
> 候选过滤、进度台账、危险识别、成功判定——这四件事全部被搬回代码之后，任务一次就过了。
> 模型现在只回答一个问题：**"这一步该由哪个元素完成"**——而这个问题恰好是 Jev 式 choice 最擅长的形态。
