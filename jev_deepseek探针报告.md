# 用 DeepSeek 复刻 Jev：延迟 / 花费 / 效果实测

> 探针脚本：`jev_probe.py`（主实验）、`jev_probe2.py`（地板延迟 + 真实规模 + 缓存）、`jev_probe3.py`（证据阶梯 + 选项规模 + RTT）
> 原始数据：`jev_probe_result.json`、`jev_probe2_result.json`
> 时间：2026-09-20（周日，off-peak）｜模型 `deepseek-flash`｜本机 KX-7000 无 GPU

---

## 0. 结论速览

| 维度 | Jev 的宣称 | DeepSeek 实测 | 判定 |
|---|---|---|---|
| **延迟** | 70–500 ms | **地板 611 ms**（36 token 进 / 5 token 出）；典型决策 0.72–1.81 s | ❌ **差 1.2–8.7 倍**，且是绕不开的地板 |
| **花费** | $0.042/M 输入，**输出免费** | 冷状态 $0.000511/次；热状态 $0.000110/次 | ⚠️ **热缓存打平，冷状态贵 4.4 倍** |
| **只出决策不出文本** | 是 | 18/18 全部是纯净 JSON，零解释性文字 | ✅ **达成** |
| **概率分布** | 255 选项上限 | N=60 全覆盖、和为 1.00、答案正确 | ✅ **达成，且没有 255 的坎** |
| **多题并行** | parallel sampler，问题隔离 | 1 次调用答 6 题，1.58 s，**隔离成立** | ✅ **达成** |
| **校准置信度** | RLCD，核心卖点 | 证据阶梯上置信度 0.875 → 0.80 → 0.65 | ✅ **方向对**（但只是自报校准，见 §6） |
| **静默失败率** | 0%（schema 保证） | 未测（需带标注的大样本） | ❓ |

**一句话**：除了**延迟**这一条硬伤，Jev 靠 prompt 能拿到的部分，DeepSeek 基本都拿到了；而延迟这一条，DeepSeek 的**网络往返（155–196 ms）就已经吃掉 Jev 宣称区间的大半**。

---

## 1. Jev 样式的 system prompt（我自己细化的版本）

664 字符，走 JSON mode（等价于 Jev 的约束解码）。

```
你是 System One —— 一个只做决策、不产出文本的判断模型。

输入: 一段【情景】, 以及一道或多道【问题】, 每题带【选项】。
输出: 一个严格 JSON 对象, 内含每题所有选项的概率分布。

决策原语(只有这三种):
- choice: 从 N 个互斥选项里选一个 -> 给出每个选项的概率分布
- noul:   是/否判断 -> 给出 {"yes": p}
- score:  给单个候选打 0~1 的相关度分 -> 给出 {"score": p}

铁律:
1. 只输出 JSON。不解释、不客套、不加代码围栏、不写"答案是"。任何解释性文字都算失败。
2. 概率必须覆盖【全部】选项, 键名与选项标签逐字一致; 不得遗漏、不得新增、不得合并。
3. choice 的概率之和必须为 1.0; noul/score 落在 [0,1]。
4. 置信度必须诚实校准: 证据弱就摊开(例如 0.35/0.35/0.30), 不要凑成 0.99。
   下游按阈值分流 —— 高置信自动执行, 低置信升级给人。虚高的置信度比答错更糟。
5. 情景是唯一事实来源。情景没写的不要脑补、不要引用常识补全。
6. 同一请求里的多道题彼此独立作答, 互不参照、互不影响、互不泄漏。

输出格式:
{"answers":[{"id":"<题号>","type":"choice|noul|score","probs":{"<标签>":<float>,...},"best":"<标签>","conf":<float>}]}
```

设计要点（对应 Jev 的三个原语 + 两条产品主张）：**铁律 4 = 校准**，**铁律 6 = 并行隔离**，**铁律 1+2 = "Decisions, not strings" 的类型契约**。

---

## 2. 延迟

### 2.1 地板延迟：DeepSeek 绕不开的下限

| 项 | 值 |
|---|---|
| 入 36 token / 出 5 token，6 次 | min **0.611 s**｜median 0.915 s｜max 1.256 s |
| 其中纯网络 RTT（curl `/v1/models`） | min 0.155 s｜median 0.166 s |
| **其中模型侧固定开销** | **≈ 0.46 s** |

**这是最硬的一条**：DeepSeek 做任何一次调用，光是把请求送达并调度起来就要 ~0.6 s。Jev 宣称的 70–500 ms 是**整个决策**的预算，而 DeepSeek 的**空转**就已经是它全额的 1.2–8.7 倍。

### 2.2 真实决策延迟（non-thinking，18 次）

| 场景 | wall | 备注 |
|---|---|---|
| 5 选项（585 token 输入） | 0.82 / 1.30 / 1.09 s | TTFT 0.589 s，64 token 输出只花 0.18 s |
| 20 选项（793 token） | 1.26 / 1.20 / 1.12 s | |
| noul / score（~510 token） | 0.72–0.95 s | |
| 双题隔离（609 token） | 1.81 / 1.16 / 1.20 s | |
| 1500 token 状态 + **单题** | 0.84–1.35 s | |
| 1500 token 状态 + **6 题一次** | 1.58 s | |
| **全 18 次** | **min 0.716｜med 1.089｜max 1.809 s** | avg 1.065 s |

**输出侧非常快**（64 token / 0.18 s ≈ 355 tok/s）；瓶颈是**固定的首 token 延迟**。这也解释了为什么加选项几乎不涨延迟——N=20 与 N=60 分别是 1.47 s / 1.67 s。

### 2.3 thinking 模式：把"推理模型太慢"钉在数据上

| 用例 | non-thinking | thinking(effort=low) | 放大 |
|---|---|---|---|
| 5 选项（简单） | 0.82–1.30 s | 1.44 / 1.44 / 1.47 s | 1.2× |
| **证据冲突（难）** | **0.93 / 1.10 / 1.04 s** | **14.94 / 8.61 / 4.83 s** | **4.6–16×** |
| 输出 token | 46 | **2913 / 1551 / 829** | 18–63× |
| 单次花费 | $0.000058 | $0.001801 / $0.000965 / $0.000532 | 9–31× |

一次请求吐了 **11267 字符的思维链**。而且**结果还不稳定**：三次分别给出 Z / Y / Z（置信 0.5 / 0.45 / 0.65）——non-thinking 三次全是 Y@0.45。

> **这正是你之前判断"推理模型做系统 1 又慢又费 token"的实测证据：简单题只慢 1.2 倍，难题慢 16 倍、贵 31 倍，而且更不一致。**

---

## 3. 花费

定价取自 [DeepSeek 官方](https://api-docs.deepseek.com/quick_start/pricing)，off-peak：`deepseek-flash` 输入 cache-miss **$0.15/M**、cache-hit **$0.003/M**、输出 **$0.60/M**。对照 Jev：输入 **$0.042/M**、输出免费。

### 3.1 单次决策成本

| 场景 | 输入 token | 输出 token | $/次 | $/10 万次 |
|---|---|---|---|---|
| 5 选项小题 | 585 | 64 | 0.000070 | $7.0 |
| 20 选项 | 793 | 169 | 0.000126 | $12.6 |
| 60 选项 | 1303 | 449 | 0.000371 | $37.1 |
| **1500 token 状态·冷** | 2730 | 169 | **0.000511** | **$51.1** |
| **1500 token 状态·热（缓存）** | 2730 | 169 | **0.000110** | **$11.0** |
| **Jev（同 token 量）** | 2730 | — | **0.000115** | **$11.5** |
| thinking·难题 | 606 | 2913 | 0.001801 | $180 |

按 `deepseek-flash` 全量 NT 均值：**$0.000082/次，约 $8.2/10 万次**。

### 3.2 两个反直觉的发现

**① 前缀缓存把 1500 token 的状态成本砍到 1/5。**
同一状态第一次问命中 384 token，之后**每次命中 2432 token**——整个状态被缓存了。于是：冷 $0.000511/次 → 热 $0.000110/次，**4.6 倍差距**。DeepSeek 的 cache-hit 单价 **$0.003/M 是 Jev 输入价 $0.042/M 的 1/14**。

**② 拆成多次问，比一次问 6 题更便宜。**

| 方式 | 花费 | 墙钟 |
|---|---|---|
| (a) 同状态 + 6 题，**一次调用** | $0.000595 | **1.58 s** |
| (b) 同状态，**拆 6 次**调用 | **$0.000502** | 6.13 s |

因为 (a) 有 2518 token 走 cache-miss，而 (b) 从第二次起 2432 token 全是 cache-hit。
**结论：Jev 的并行采样器省的是"回合数"（延迟），在 DeepSeek 上反而费钱。** 若换成 Jev，6 次独立调用要 6×2730×$0.042/M = $0.00068 —— **热缓存下 DeepSeek 比 Jev 便宜 26%**。

---

## 4. 效果达成度（"像不像 Jev"）

### 4.1 输出契约：18/18 全部通过

- **JSON 解析成功率 100%（18/18）**，无一例夹带解释性文字或代码围栏
- 概率和：choice 恒为 1.00（有一次 1.03、一次 0.99）；noul/score 按约定只给单侧值

### 4.2 20 选项与 60 选项：全覆盖，且没有 255 的坎

```json
// N=20, best=E10(0.72)，20 个选项全部给出概率
{"E1":0.01,"E2":0.02,...,"E9":0.03,"E10":0.72,"E11":0.06,...,"E20":0.01}
```

| N | 覆盖率 | 概率和 | 正确答案 | 延迟 | 花费 |
|---|---|---|---|---|---|
| 20 | **20/20** | 0.99 | O20 @0.80 ✅ | 1.47 s | $0.000156 |
| 60 | **60/60** | 1.00 | O42 @0.94 ✅ | 1.67 s | $0.000371 |

**选项数从 5 涨到 60，延迟只涨 1.4 倍、花费涨 5 倍，覆盖面没有损失。** Jev 的 255 上限在这个路线里不是能力边界，只是输出 token 的成本问题。

### 4.3 多题并行 + 问题隔离：成立

一次调用问两题（`q1: 下一步点哪个元素` / `q2: 此刻点保存能成功吗`）：

```json
{"answers":[
  {"id":"q1","best":"E2","probs":{"E1":0.02,"E2":0.94,"E3":0.02,"E4":0.02},"conf":0.94},
  {"id":"q2","best":"no","probs":{"yes":0.02,"no":0.98},"conf":0.98}]}
```

q2 的答案与 q1 完全无关（保存按钮 disabled，所以是 no@0.98）——**没有污染**。3/3 次一致。
扩到 6 题一次调用同样 6/6 正确（`E02@0.85`、`no@0.98`、`no@0.95`、`后端@0.70`、`0.85`、`11@0.95`）。

### 4.4 校准：置信度**跟着证据动**（关键正面结果）

同一情景，只增删**一条消歧事实**：

| 变体 | 改动 | best | conf | P(目标) |
|---|---|---|---|---|
| 强证据 | `当前选中行: 10.0.3.7`（就是目标机） | E2 | 0.85 / 0.90 | 0.85 / 0.90 |
| 反证据 | `当前选中行: 10.0.3.9`（是另一台） | E3 | 0.80 / 0.80 | 0.05 |
| **无消歧事实** | 删掉该行 | E3 | **0.65 / 0.65** | 0.15 |

**强/悬空置信差 = 0.22**，且方向正确：证据消失 → 置信下滑、目标概率被摊到 0.15。
更好的信号是**反证据**那一栏：事实说"当前选中是另一台"，模型没有硬点重启，而是改去点 E3（先选对行）——这是**行为**层面的正确，不只是概率层面。

冲突情景（两条规则互相矛盾）也没硬凑：

```json
{"X":0.25,"Y":0.45,"Z":0.30}   // 三次完全一致
```

### 4.5 确定性

`temperature=0` 下 3 次重复**逐字一致**（A–F 六个用例、18 次调用，同一用例三次输出相同）。对工程是有利的：可回归、可快照测试。代价是**丧失了 Jev 那种"同一状态采多个样本"的语义**——本路线要用多样性只能靠改 prompt。

---

## 5. 一张总表

```
                        地板(36tok)   5选项    20选项    1500tok单题   6题一次   thinking难题
延迟 wall (s)              0.61       0.82     1.12       0.84         1.58      4.83–14.94
延迟 相对 Jev 上限500ms     1.2×       1.6×     2.2×       1.7×         3.2×      9.7–29.9×
花费 $/次 (DeepSeek)    0.000008   0.000070 0.000126   0.000511冷    0.000595   0.00053–0.0018
花费 $/次 (Jev 同 token) 0.000002   0.000025 0.000033   0.000115      0.000122     0.000025
```

> Jev 那一行按 $0.042/M×输入 token 计算、输出免费。**"输出免费"是 Jev 相对 DeepSeek 唯一的结构性价格优势**——20 选项场景里输出已占单次成本的 **55%（冷）到 80%（热）**（169 token × $0.60/M = $0.000101）。

---

## 6. 局限（必须说清）

1. **我没有调用真 Jev。** `api.typesafe.ai` 从本机可达（GET 返回 405，正常），但没有 TypeSafe 的 key，"Jev 效果"一栏全部是**按其公开宣称**对照，不是同题对打。**要下最终结论，应该拿同一批用例打真 Jev。** 拿到 key 我可以直接复用这套探针。
2. **"校准"我只证明了置信度随证据变化（自报校准），没有测统计校准（ECE）。** 后者需要几百条带标注样本 + 分箱统计。这是**唯一能真正验证/推翻 Jev 卖点**的实验，也是你项目里最该做的一个。
3. 中文 prompt + 中文状态。Jev 可能在英文上更强。
4. 单次机器、单次网络环境；延迟受本机到 DeepSeek 的链路影响（RTT 155–196 ms）。
5. `temperature=0` 保证了可复现，但也说明这次实验**没有测到采样方差**。

---

## 7. 对你项目的直接结论

1. **延迟必须由本地小模型承担，不能指望云端 API。** 611 ms 的地板决定了"快思考"这一步走 DeepSeek 是走不通的；但**云端做系统 2 / 升级路径完全够用**（0.8–1.8 s，$0.0001/次）。
2. **成本上"热缓存"是关键杠杆。** 桌面状态只要保持稳定前缀，1500 token 状态从 $0.0005 降到 $0.0001——这样 10 万次决策 ~$11，**跟你之前用 Jev 当 teacher 标注 10 万条 ~$6.3 是同一量级**。换句话说：**DeepSeek 本身就能当 teacher，成本不比 Jev 贵**。
3. **Jev 那套 prompt 是能抄的，而且抄完效果相当好。** 18/18 schema、60/60 覆盖、并行隔离、证据阶梯 0.22 的置信差——这些都是 **prompt + JSON mode** 换来的，不需要特殊模型。这印证了你上一轮的判断：**技术创新不多，价值在接口和定价**。
4. **真正没被拿到、也最值得你做的，是统计校准（ECE）。** 自报置信度会动 ≠ 置信度可信。你项目里的阈值分流（自动执行 / 升级给人）全靠这个数字，所以**它必须被测量，而不是被相信**——这也正好是 kev 那类本地小模型 + 温度缩放能建立真正差异化的地方。
