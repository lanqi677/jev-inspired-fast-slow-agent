# 打包说明与换设备恢复指南

> 打包时间：2026-09-20 ｜ 源目录：`~/事务/研究jev`
> **本仓库是脱敏后的公开副本**，见下方「零、脱敏说明」。

---

## 零、脱敏说明（与原始工作目录的差异）

原始研究跑在两个**内网后台网页**上，原始包里带着真实域名、真实员工数据与页面截图。
本副本在发布前做了以下处理，**方法与全部结论一字未改**：

| 处理 | 具体做法 |
|---|---|
| **内网产品名隐去** | 两个内网系统在本文档与代码里统一称为 **SiteA / SiteB**（代码 id：`sitea` / `siteb`），即"相关网页" |
| **域名占位** | 真实域名 → `sitea.internal.example` / `siteb.internal.example`；此后缀不可解析，**照抄命令跑不通真实站点，这是有意的** |
| **内网 IP 占位** | 真实内网地址段 → `10.0.0.11` / `10.0.0.12` |
| **真实工号 / 人名 / 邮箱** | 换成假值（如工号 `10000001`、`zhangsan@internal.example`） |
| **侦察记录删除** | `general/recon_*.{json,png}`、`general/recon_run.log`、`general/headed_probe.log` **已从本副本删除**：它们含真实成员列表、真实姓名邮箱与内网页面截图 |
| **浏览器 profile** | 从不入包（含内网 Cookies / Login Data） |
| **无关第三方代码** | `terrablind/`（早期参考材料，非本项目代码）已剔除 |

> 因此：**mock 路线（`OPS_MOCK=1`）与贪吃蛇路线在本副本里可以完整复跑**；
> 但"打真实内网"那几条命令需要你自己把占位域名换回真实地址、并手工登录后才可用。

---

## 一、包里有什么

```
研究jev/
├── README.md                         ← 先看这个（思路 + 结论）
├── README-总览.md                    详细总览
├── general/                          ← 引擎与适配器（核心成果）
│   ├── engine.mjs                    引擎本体（739 行，零领域代码）
│   ├── adapter.mjs                   适配器层（统一表示）
│   ├── adapters/{sitea,siteb,snake}.json  三个适配器实例
│   ├── browser.mjs                   常驻浏览器管理
│   ├── games/snake.html              贪吃蛇测试台
│   ├── mocks.json                    真实地址 → 本地 mock 的映射表
│   ├── verify_adapter.mjs            适配器量化验证脚本
│   ├── snake_matrix.mjs              A/B 回归：同种子跑不同任务描述
│   ├── snake_policy_probe.mjs        纯代码策略探针（不调模型，零花费）
│   ├── task_*.txt                    任务描述（自然语言）
│   ├── 泛化性验证报告.md              最新一轮的泛化性结论
│   └── 贪吃蛇策略改进验证.md          ⭐ 只改描述能改多少策略（含一次诊断推翻）
├── browser_jev/                      早期两阶段原型（感知层 perceive.js 仍在用）
├── ops_scenario/                     两个内网页的等价 mock（不联内网就能跑）
├── refs/                             生态插件 README 快照
├── *.md                              各轮验证报告
├── *.py / verify_*.py                所有数字的原始脚本
└── jev控制台.html                    独立的决策控制台（可视化体验用）
```

---

## 二、包里**没有**什么（以及新设备怎么补）

### 1. 🔴 浏览器 profile —— **故意排除，不要试图复制**

| 排除的 | 原因 |
|---|---|
| `general/.chrome-real/`（68M） | **里面有内网 Cookies 和 Login Data**（SiteA 的 SSO 会话、SiteB 的登录票据）。这是凭证，不该随包外带；而且换设备后也未必有效。 |
| `general/.chrome/`、`.chrome-headed/` | 临时 profile 缓存，会自动重建 |

**新设备上重新登录即可**：
```bash
cd general && node browser.mjs start "https://sitea.internal.example/project/#/team/TEAMID/team_setting/department/all_member"
# 在弹出的窗口里手工登录（脚本不碰你的密码）
# 登录态会存进 .chrome-real/，之后长期有效
```

### 2. 本地模型工具链 —— 已验证用不上，结论已进文档

| 排除的 | 大小 | 为什么可以不要 |
|---|---|---|
| `models/` | 4.5G | Qwen GGUF。**已实测：本机纯 CPU 跑不动 Jev 式决策**（1.9–11.9 秒/次） |
| `tools/` `src/` `tmp/` `pylibs/` | 1.13G | 编译 llama.cpp 的工具链。**没有任何脚本引用 pylibs**（只有 `run_*.sh` 的本地基准用到） |
| `llama.cpp/` | 3.8M | 同上 |

结论都在 `jev本机可行性报告.md` 里，不需要重跑。

### 3. 凭证文件 —— **不在本项目里，本来就不会被打包**

引擎的 API key 读自 **`~/.dsh/.credentials.yaml`**：
```yaml
DEEPSEEK_API_KEY: sk-xxxxxxxx
```
**新设备上需要自己创建这个文件**，或者改 `engine.mjs` 顶部那行 `KEY` 的读法。

### 4. 其它

- `__pycache__/`、`:memory:.ses`、`.body.json` —— 缓存/临时文件

---

## 三、新设备上手步骤

### 依赖

| 依赖 | 要求 | 检查 |
|---|---|---|
| **Node.js** | **≥ 22**（要全局 `WebSocket` 和 `fetch`） | `node -v` |
| **Chrome for Testing** | 152.0.7977.75（或自己指定） | 见下 |
| **DeepSeek API key** | `~/.dsh/.credentials.yaml` | 见上 |
| **内网访问** | 只有跑真实 SiteA/SiteB 才需要 | `curl -I https://sitea.internal.example` |

### Chrome 的两种形态（两个路径都要）

引擎按模式用不同的 Chrome：

```bash
# ① 无头版（mock 回归用）
~/.cache/puppeteer/chrome-headless-shell/linux-152.0.7977.75/chrome-headless-shell-linux64/chrome-headless-shell

# ② 完整版（常驻浏览器 / 真实站点用）
~/.cache/puppeteer/chrome/linux-152.0.7977.75/chrome-linux64/chrome
```

**装法**（任选其一）：
```bash
npx @puppeteer/browsers install chrome@152.0.7977.75
npx @puppeteer/browsers install chrome-headless-shell@152.0.7977.75
# 装到别处也行，用环境变量指过去（不用改代码）：
export CHROME_BIN=/path/to/chrome
export CHROME_HEADLESS_BIN=/path/to/chrome-headless-shell
export CHROME_VERSION=152.0.7977.75     # 换版本号也行
```
解压后如果报权限错误：`chmod +x chrome chrome_crashpad_handler chrome_sandbox`

### 自检（三条命令，按顺序）

```bash
cd general

# ① 不联内网、不登录：mock 六分支之一 —— 应该输出「问题1：工号为空…」
OPS_MOCK=1 OPS_V=empty_id OPS_DV=ok TASK_FILE="task_账号排查.txt" node engine.mjs

# ② 用手工登录后的常驻浏览器，打真实内网 —— 应该输出「工号 10000001 在 SiteA 中存在…」
ATTACH=1 CDP_PORT=9240 ADAPTER=sitea TASK_FILE="task_sitea工号存在性.txt" node engine.mjs

# ③ 换一个完全不同的领域：网页贪吃蛇（自动进 loop 模式）
ATTACH=1 CDP_PORT=9240 ADAPTER=snake TASK_FILE="task_贪吃蛇.txt" node engine.mjs
```

**三条都过 = 环境恢复完毕。**

---

## 四、如果新设备在**外网**（连不上内网）

只有 ① 和部分 ③ 能跑：
- ✅ `OPS_MOCK=1` 的六分支回归 —— 完全离线
- ✅ 贪吃蛇 —— 完全离线
- ❌ 真实 SiteA / SiteB —— **需要内网 + 手工登录**，做不到就是做不到

好消息是：**引擎的正确性验证不需要内网**。真实的 SiteA 页面结构已经被固化进 `adapters/sitea.json`，回归主要靠 mock。

---

## 五、⚠️ 已知未修复缺陷（交接重点）

### 缺陷 #1：`dup_email` 分支间歇性给出**错误结论**

> **状态：未修复，根因未定位。经决定不在此轮修复，原样交接。**

#### 现象

| 项 | 值 |
|---|---|
| 复现命令 | `OPS_MOCK=1 OPS_V=dup_email OPS_DV=ok TASK_FILE="task_账号排查.txt" node engine.mjs` |
| **期望** | `问题2：邮箱重名，该邮箱属于别人` |
| **实际（偶发）** | `正常：系统间更新间隔一天，等明天再看` |

注意错误答案的性质：**「正常」是 SiteB 分支的结局**。也就是说，这次运行**走去了 SiteB**，而没有在 SiteA 详情页的"姓名不一致"处停下。

#### 正确路径应该是什么

```
1. SiteA 搜工号 123            → 计数 0
2. SiteA 搜邮箱 123@123.com    → 计数 1
3. 点「查看详情」             → 工号=888（不为空）、姓名=李四
4. 同名判断(李四, 张三)       → false
5. 命中「问题2」→ 终止
```

mock 数据（`ops_scenario/mock_sitea.html:46`）：
```js
dup_email: { byId:{}, byEmail:{'123@123.com':[{name:'李四',email:'123@123.com',empId:'888'}]} }
```

#### 实测频次

| 场景 | 结果 |
|---|---|
| 5 分支背靠背连跑（矩阵） | **23 / 25 ≈ 92%** |
| 该分支单独跑 | **约 17 过 / 1 败 ≈ 94%**（其中两次连续 5/5、10/10） |
| 最后一次触发 | 在 `/tmp/packtest/` 解压副本里做自检①时触发 |

**结论：与负载无关，就是间歇性的**（之前一度以为和并发有关，后来在单跑里也复现了）。

#### 🔴 最大的障碍：**失败的那次日志从来没被保存下来**

我尝试过三轮抓取（同一分支连跑 8–10 次、保留全量日志），**每次都全部通过**，一次都没抓住。
所以下面这些**全部是未验证的假设**，不是结论。

#### 未验证的假设（供接手人参考，不要当结论用）

| # | 假设 | 支持/反对 |
|---|---|---|
| a | **规划器把「姓名相同→进 SiteB」和「姓名不同→问题2」挂反了，或漏了后者** | **最可能**。已观察到同一个任务每次拆解都不一样（事实编号、规则条数、规则 id 都在变） |
| b | `sameName('李四','张三')` 偶发返回 `true` | 反对：所有抓到的样本都是 `false conf=0.99` |
| c | 抽取器把详情页「姓名」抽成了 `张三` | 反对：抽取调用**不含任务描述**，只看页面文字，没有"抄任务里期望值"的渠道 |
| d | 计数事实采到了防抖中间态，走进了错误分支 | 未验证。适配器的 `pollCountStable` 在真实站点上实测是稳的 |
| e | 规则顺序问题：SiteB 分支规则先命中 | 反对：它引用的事实（SiteB 邮箱/更新日期）那时根本没被采集 |

**假设 a 的证据链**：`checkPlan` 已经能拦住**结构性**缺陷（词表外的操作符、没人 snapshot 的事实、类型不符的元素），但拦不住**语义**错误——比如"把结论挂到了错误的规则上"。而该分支恰好需要一条正确的「姓名不同 → 问题2」规则才能收口。

#### 为什么这一条很重要

它不是一个孤立的 mock 问题，它暴露的是整条链路的**可靠性上限**：

- 引擎对**结构性**错误已经有闸（`checkPlan`、`FIT` 元素类型、护栏）
- 但对**语义**错误**完全没有防护**——而且这类错误**答案是错的、看起来却是合理的**
- 现有架构里本来有个 `verify()`（拿已冻结事实复核结论）就是干这个的，**但它从没被调用过，是死代码**（见下）
- 用户设想的闭环「置信度低/失败 → 推理 LLM 介入」**目前接不上这一环**：因为这里的失败是**静默的**（规则正常命中、结论正常输出），没有任何信号宣告"我不确定"

#### 接手后怎么抓（这是第一件该做的事）

**加结构化运行日志**——把每次运行的这几样东西落盘成 JSON：

```jsonc
{
  "task": "…", "adapter": "…", "seed": 12345,
  "plan": { "facts": [...], "rules": [...], "actions": [...] },   // 规划器原始输出，不要只打摘要
  "steps": [ { "i": 1, "action": "…", "selected": "J1", "conf": 0.97, "changed": true } ],
  "factsAtFire": { "f1": 0, "f2": 1, "f5": false },               // 命中规则那一刻的冻结事实
  "firedRule": "r5", "conclusion": "…"
}
```

然后**失败时直接 diff 失败样本与通过样本的 `plan.rules`**——如果是假设 a，diff 会立刻显形。

顺带这件事还能解决另外两个问题（见下）：稳定性度量、以及"为什么混合连跑比单跑差"。

---

### 缺陷 #2：`verify()` 是死代码——结论没有任何第二道校验

`engine.mjs` 里有一个 `verify()`，设计意图是"拿已冻结的事实复核结论是否自洽"，用的是 `deepseek-v4-pro`。

```bash
grep -n "async function verify\|await verify" general/engine.mjs
# 427:async function verify(...)      ← 只有定义
# （没有任何调用点）
```

**当前所有结论都由命中的规则直接给出，没有经过复核。**

需要注意的是：**接上它也不一定救得了缺陷 #1**——那次的结论与（可能算错的）事实是自洽的，复核会同意它。
但它能拦住另一类错误：**规则与结论的对应关系写错**（正好是假设 a 的形态）。

**建议**：单独验证一轮再决定接不接（用已知结论反推），不要顺手打开。

---

### 缺陷 #3：常驻浏览器偶发卡死

- 现象：`node browser.mjs status` 读不到，`/json/version` 超时
- **已缓解**：`engine.mjs` 在 `ATTACH=1` 时会检测并**自动重新拉起**（登录态在 profile 里，不用重新登录）
- **根因未定位**：不是 OOM（`free` 有 6G 余量、无 OOM 记录），不是父 shell 带走（`browser.mjs` 用了 `detached + unref`），实测跑 5 次 mock 也不会杀它

---

## 六、研究停在哪 / 下一步

当前状态（详见 `general/泛化性验证报告.md`）：

- ✅ 三层架构跑通：**同一份引擎**，dom 型适配器（真实内网后台）+ custom 型适配器（网页游戏）
- ✅ 引擎里**零领域专有代码**（可 grep 验证）
- ✅ 回归基线：mock 混合连跑 23/25；真实 SiteA 3/3；贪吃蛇目标 5 分 2/3
- ⚠️ **有一个未修复的间歇性错误结论**（缺陷 #1，见上）

**下一步（按依赖顺序）**：

| | 做什么 | 为什么排这个位置 |
|---|---|---|
| **0** ⭐ | **先加结构化运行日志** | **缺陷 #1 现在抓不住，就是因为没有日志**。不加这个，后面每一步都在盲改 |
| 1 | **钉死缺陷 #1** | 它是当前唯一已知的"答案是错的、看起来却合理"的失败。见上文的假设表 |
| 2 | **统一 plan 和 loop 两种模式** | 推理 LLM 只产出「事实+规则」，快决策逐步选动作，代码每步后算事实判规则 |
| 3 | **给引擎加"是否被围死"的逐步标记** | 贪吃蛇那轮只知道最终死因（见 `general/贪吃蛇策略改进验证.md`）|
| 4 | **适配器生成流程固化** | 三个适配器都是推理模型写的，但还是一条条现写 |
| 5 | **接回 `verify()` 并单独验证** | 见缺陷 #2，不要顺手打开 |

---

## 七、本机环境备忘

| 项 | 值 |
|---|---|
| 硬件 | 兆芯 KX-7000，8 核 8 线程，**2×4 核集群**，无 GPU，13G 内存 |
| 本地模型注意 | 必须 `taskset -c 0-3 -t 4`；8 线程反而崩（13.44 → 5.47 tok/s） |
| Node | v22.22.3 |
| 内网（占位） | `sitea.internal.example` → 10.0.0.11；`siteb.internal.example` → 10.0.0.12（真实值已隐去，见 §0） |
