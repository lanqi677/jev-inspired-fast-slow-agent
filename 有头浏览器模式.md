# 有头浏览器模式 —— 想监控就打开，不想监控就最小化

> 结论：**能，而且最小化之后自动化照跑不误。** 已实测。

---

## 1. 三个模式

| 模式 | 环境变量 | 你能看到什么 | 用途 |
|---|---|---|---|
| 无头 | `BROWSER=headless`（默认） | 什么都看不到 | 无人值守、跑批 |
| **有头** | `BROWSER=headed` | **一个真实的 Chrome 窗口** | **你想盯着看的时候** |
| 有头+启动即最小化 | `BROWSER=headed START_MIN=1` | 任务栏一个图标 | 不想被窗口打扰，但随时能点开看 |

```bash
# 想看着它干活
BROWSER=headed TASK_FILE="task_账号排查.txt" node engine.mjs

# 不想被打扰，但随时能点开看
BROWSER=headed START_MIN=1 TASK_FILE="task_账号排查.txt" node engine.mjs
```

---

## 2. 关键实测：最小化之后还能不能跑？

这是唯一真正需要验证的问题。因为感知层靠 `getBoundingClientRect()` 的尺寸来过滤"可见元素"
（`r.width >= 4 && r.height >= 4`）——**如果最小化让尺寸变成 0，整个 agent 会瞬间"瞎掉"，一个候选元素都看不到。**

实测结果（`headed_probe.mjs` 的输出）：

| 观察项 | 正常显示 | **最小化中** |
|---|---|---|
| 窗口状态 | `normal` | **`minimized`** |
| `document.hidden` | `false` | **`true`** |
| `document.visibilityState` | `visible` | **`hidden`** |
| `document.hasFocus()` | `true` | **`false`** |
| 输入框 `getBoundingClientRect()` | `[119,73,342,34]` | **`[119,73,342,34]`** ← **一模一样** |
| 感知层枚举到的候选元素 | 正常 | **正常** |
| 搜索结果 | — | **0 行 → 1 行 ✅** |
| 点「查看详情」 | — | **OK，面板已打开 ✅** |
| 还原后 | — | **一切正常 ✅** |

**为什么没事**：布局（layout）和窗口是否可见是两回事。最小化只是不合成画面，DOM 与布局照常。
而且我们的自动化是**语义操作**（`element.click()`、`el.value=…`、`querySelectorAll`），
**不依赖坐标、不依赖画面**——所以最小化完全不影响。

> 反过来说：**如果当初选了"截图 + 坐标点击"的路线，这条就过不去了**——最小化后截不到图，也没法把鼠标移到窗口上。
> 又一次证明 DOM 语义路线的价值。

### 唯一的已知差异

Chrome 会**节流后台标签页的定时器**（`setTimeout` 最小 1s）、**暂停 `requestAnimationFrame`**。
所以：
- ✅ 我们的做法（DOM 查询、`.click()`、设值、派发事件）**不受影响**
- ⚠️ 如果目标页面自身依赖 `rAF` 或短定时器来渲染内容（**虚拟滚动、懒加载、图表动画**），最小化时可能**不刷新**，导致看不到新内容

**对策**（如果真遇到）：宁可让它**最大化/普通显示但在另一个虚拟桌面**，或者干脆不用最小化而用窗口位置移到屏幕外：
```bash
node window.mjs move -3000 -3000    # 移到屏幕外（部分 WM 支持）
```

---

## 3. 运行时随时开关：`window.mjs`

任务正在跑的时候，另开一个终端：

```bash
node window.mjs          # 看当前窗口状态、位置尺寸、正在看哪个页面
node window.mjs min      # 最小化（自动化继续跑）
node window.mjs restore  # 还原
node window.mjs max      # 最大化
node window.mjs focus    # 提到前台
node window.mjs close    # 关掉浏览器（会中断当前任务）
```

实测输出：
```
$ node window.mjs info
windowId : 1686004180
状态     : normal
位置尺寸 : 100,35  1200×801
页面     : SiteA · 全部成员  file:///…/mock_sitea.html?v=dup_email

$ node window.mjs min
窗口状态 → minimized
```

它连的是**浏览器级 CDP 端点**（`/json/version` 的 `webSocketDebuggerUrl`），
和正在跑任务的**页面级连接互不干扰**——所以你可以一边跑一边切。

**当然，最省事的做法是直接用鼠标点窗口的最小化按钮。** 这只是给你一个脚本化的入口。

---

## 4. 装了什么

| 项 | 位置 | 大小 |
|---|---|---|
| 有头 Chrome（Chrome for Testing 152） | `~/.cache/puppeteer/chrome/linux-152.0.7977.75/chrome-linux64/chrome` | 391 MB |
| 无头 chrome-headless-shell（原有） | `~/.cache/puppeteer/chrome-headless-shell/…` | 261 MB |

从 `cdn.npmmirror.com` 下载（**9.4 MB/s**，官方源只有 185 KB/s）。

> **装的时候踩到一个坑**：zip 解压后 `chrome_crashpad_handler` 等辅助程序**丢掉了可执行位**，
> 启动时报 `posix_spawn … 权限不够 (13)`。修法：
> ```bash
> chmod -R u+rwX,go+rX ~/.cache/puppeteer/chrome/linux-152.0.7977.75/chrome-linux64
> chmod +x ~/.cache/puppeteer/chrome/linux-152.0.7977.75/chrome-linux64/{chrome,chrome_crashpad_handler,chrome_sandbox}
> ```

---

## 5. 顺带一个对真实场景很重要的提醒

有头 + 你能看到窗口，意味着**登录/SSO 可以人工介入**。

真实 SiteB 的「点登陆」如果跳 SSO（短信验证码、双因子、扫码），自动化会卡住。
有头模式下你可以：

1. 让 agent 跑到那一步卡住
2. **你手动在窗口里把登录做完**
3. agent 下一步重新感知时，看到的就是已登录的页面，继续往下走

这是**无头模式做不到**的。所以对内部的、要走 SSO 的系统，**建议默认就有头模式**，
把它当成"半自动"而不是"全自动"——这也和生态里 `wqty123/dsh-browser` 宣传的"用户可随时接管"是同一个思路。

---

## 6. 文件

| 文件 | 作用 |
|---|---|
| `general/window.mjs` | 运行时窗口控制（info/min/restore/max/focus/close） |
| `general/headed_probe.mjs` | 最小化前后行为的实测脚本（可复跑验证） |
| `general/headed_test.mjs` | 早期版本，功能被 probe 覆盖 |
| `general/engine.mjs` | 引擎，新增 `BROWSER=headed` / `START_MIN=1` |
